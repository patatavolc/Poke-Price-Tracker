import { query } from "../config/db.js";

export const getCardDetails = async (req, res) => {
  const { id } = req.params;
  try {
    // Obtener la carta y su historial de precios
    const cardQuery = ""
  } catch (error) {
    
  }
}